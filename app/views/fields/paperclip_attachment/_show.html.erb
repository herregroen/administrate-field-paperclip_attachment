<%#
# Paperclip Image Show Partial
This partial renders a paperclip image attribute,
to be displayed on a resource's show page.
By default, the image is rendered.
## Local variables:
- `field`:
  An instance of Administrate::Field::PaperclipImage.
  A wrapper around a paperclip attachment pulled from the database.
%>

<% if field.show? %>
  <% if field.is_image %>
    <%= image_tag field.url_for(:show) %>
  <% else %>
    <%= link_to field.data.original_filename, field.url_for(:show) %>
  <% end %>
<% end %>
